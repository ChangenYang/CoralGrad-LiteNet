# Weights

Put released checkpoints here if you want to publish weight files.

Example:

- `weights/best.pt`

This repository is already structured so it can be used either as:

- a core-code release
- a weight-only release
- a core-code plus weights release
